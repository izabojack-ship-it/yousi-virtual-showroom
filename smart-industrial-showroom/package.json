{
  "name": "smart-industrial-showroom",
  "private": true,
  "version": "1.0.0",
  "description": "智慧工業雲端展間 — Industry 4.0 AR Showroom Monorepo",
  "workspaces": [
    "apps/web",
    "services/image-processor"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:processor": "npm run dev --workspace=services/image-processor",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
